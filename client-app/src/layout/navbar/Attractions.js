import { Helmet } from "react-helmet";

const Attractions = () => {
    return (
        <div>
            <Helmet>
                <title>Booking Attractions Page</title>
            </Helmet>
            <h2>Attractions</h2>
        </div>
    );
};

export default Attractions;
