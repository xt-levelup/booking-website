import { Helmet } from "react-helmet";

const Flights = () => {
    return (
        <div>
            <Helmet>
                <title>Booking Flights Page</title>
            </Helmet>
            <h2>Flights</h2>
        </div>
    );
};

export default Flights;
