import { Helmet } from "react-helmet";

const Users = () => {
    return (
        <div>
            <Helmet>
                <title>Booking Admin Users Page</title>
            </Helmet>
            <h2>Users</h2>
        </div>
    );
};

export default Users;
